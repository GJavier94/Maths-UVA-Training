#include <bits/stdc++.h>

using namespace std;

typedef pair<double,double> point;

typedef vector<vector<point> > vii;

const int n = 1000;

int main(){

	int v,t;

	while(cin >> v >> t && (v || t)){
		double xs, ys, xt, yt, radio;
		radio = v*t*60;
		cin >> xs >> ys >> xt >> yt;
		vii edges(n);
		


	}
}